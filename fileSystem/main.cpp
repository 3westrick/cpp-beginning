#include "iostream"
#include "filesystem"

///The <filesystem> library (introduced in C++17) provides powerful tools for:<br>
/// ✔️ Creating, removing, and checking files<br>
/// ✔️ Listing files in a directory<br>
/// ✔️ Checking file sizes and existence

int func()
{
    return 0;
}

int main()
{
    /// Checking if a File Exists
    std::string filename = "example.txt";
//    if (std::filesystem::exists(filename)) {
//        std::cout << "File exists!" << std::endl;
//    } else {
//        std::cout << "File does not exist." << std::endl;
//    }

    int x = func();
    return 0;
}